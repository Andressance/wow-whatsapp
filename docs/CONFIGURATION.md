# Configuration

> **Responsibility notice:** use this project at your own risk. It is not
> affiliated with Blizzard or WhatsApp and its external bridge may not comply
> with their policies. You are responsible for your accounts, data and use.

The bridge reads `bridge/config.json`, normally generated by `node setup.js`.
The file is local and ignored by Git. Restart the bridge after editing it.

## WhatsApp

| Key | Default | Meaning |
|---|---|---|
| `whatsapp.enabled` | `true` | Enable the WhatsApp Web provider. |
| `whatsapp.authPath` | `.wwebjs_auth` | Local QR-authentication session. Never commit this folder. |
| `whatsapp.headless` | `true` | Hide the Chromium WhatsApp Web window. |

The bridge uses `whatsapp-web.js`; no Meta, Green API or other third-party
account is required. On first start scan the QR code in WhatsApp > Linked
devices.

## Paths and transport

| Key | Meaning |
|---|---|
| `addonDir` | WoW `Interface\AddOns` folder. |
| `inboxFile` | Reload fallback, normally `WoWWhatsApp\Inbox.lua`. |
| `savedVariablesFile` | WoW saved variables file used by the reload fallback. |
| `defaultCwd` | Legacy working-folder setting retained for transport compatibility. |
| `capture` | Screen-capture settings for the pixel transport. |
| `slots` | Number of load-on-demand inbound slot addons. |
| `incomingMax` | Number of incoming WAV signal positions. |
| `presenceMax` | Number of bridge-presence signal positions. |
| `tocInterface` | WoW interface number written into generated slot TOCs. |

The addon cannot access the network or filesystem. Messages therefore use the
pixel strip from WoW to the bridge and Lua slot files/signals from the bridge to
WoW. If the sound channel is unavailable, incoming slots are polled. `/reload`
is still available as a fallback.

## Display labels

Non-text WhatsApp content is represented safely:

- `[E]` emoji-only, `[S]` sticker
- `[P]` photo, `[V]` video, `[A]` audio
- `[L]` location, `[C]` contact, `[D]` document
- `[O]` poll, `[R]` reaction, `[U]` unsupported content

Captions remain after photo, video and document labels.

## Terminal commands

```powershell
npm install
node setup.js
npm start
node bridge\install-slots.js
```

The installed command is `wow-whatsapp`; `wow-whatsapp` remains as a legacy
alias. `bridge\start-window.cmd` starts the bridge in a separate window.
